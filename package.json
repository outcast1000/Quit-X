{
  "name": "block-twitter",
  "version": "1.0.0",
  "scripts": {
    "build:prod": "cross-env NODE_ENV=production node build.js",
    "build:debug": "cross-env NODE_ENV=debug node build.js"
  },
  "dependencies": {
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}